# bing-daily-expenses · V1

Commit summary: `feat: launch 饼饼记账 static V1 with local ledger and reviewed screenshot entry`

- 新建独立静态资产，运行文件为 index.html、style.css、app.js；中文移动端界面与首页 / 记账 / 统计 / 明细底部导航。
- 快速记、补记、批量补记；支出 / 收入，完整一级与二级分类、最近使用默认。
- 交通含上下班打车；交通卡充值归入生活缴费。
- 旅行作为独立标签，支持跨分类筛选和汇总，不重复计算支出。
- 今日 / 本周 / 本月支出、本月收入、结余，周 / 月 / 年统计与子栏目展开。
- 明细筛选、搜索、编辑、删除；JSON 备份导出与按 ID 去重合并导入。
- 截图本地预览、Tesseract.js OCR 与文字粘贴解析；识别 / 手动辅助结果只进入待确认，逐笔确认后入账。
- 金额用整数分存储；日期、分类及备份结构校验；存储失败提示，损坏数据暂停写入。

## 部署

将三个运行文件放入 GitHub 仓库根目录，或保留在 bing-daily-expenses 子目录。仓库 Settings → Pages → Deploy from a branch，选择 main 分支及根目录。根目录部署后直接访问 Pages 地址；子目录使用 `/bing-daily-expenses/`。无需构建、安装依赖或配置 API 密钥。

## 数据与 OCR

localStorage 按浏览器、设备和网站域名隔离，不自动云同步。请定期导出 JSON；更换设备或域名后导入备份。截图图片不持久保存，提取文字会保存到待确认备注。OCR 首次加载需要网络访问 jsDelivr 与语言模型资源，计算在浏览器中进行；不可用时粘贴手机提取文字或手动建立待确认。每次文字解析生成一笔记录，多金额暂取第一个，必须检查金额、日期、商家和分类。

## 验证

JavaScript 语法检查通过；金额精度、非法金额 / 日期拒绝、编辑 ID 与创建时间保留、待确认隔离、周一周起点、文本转义和分类规则检查通过。浏览器视觉与真实截图 OCR 尚未实机验证。
